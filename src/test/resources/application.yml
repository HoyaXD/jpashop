spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop;
#    username: sa
#    password:
  #    driver-class-name: org.h2.Driver

  #  jpa:
  #    hibernate:
  #      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
# spring boot에는 위에 필요없다
# 당연히 분리해야한다 운영에서의 설정이 다르다.
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
